<%= link_to "返回", myasset_t1topics_path %>

<div class="row">
    <% @topicsundert1.each do |topic| %>
    <div class="row">
<%= link_to myasset_topic_newvideo_path(topic), :class => "btn btn-primary text-center  t2-topic", :data => {:trackevent => "create_video_topic"} do %>
            <%= topic.title %> <i class= "fa fa-video-camera" aria-hidden="true"></i>
        <% end %>
    </div>
    <br>
        
    <% end %>
</div>
     