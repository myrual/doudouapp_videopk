<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @stream.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @stream.content %>
</p>
      <%= render :partial => "common/battleinstream", locals: {stream: @stream}%>

<%= link_to 'Edit', edit_myasset_stream_path(@stream) %> |
<%= link_to 'Back', myasset_streams_path %>
